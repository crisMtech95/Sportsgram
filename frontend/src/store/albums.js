import { csrfFetch } from "./csrf";













const initialState = {};

const albumsReducer = (state = initialState, action) => {
    let newState = {};
    switch(action.type) {
        default:
            return state;
    }
}

export default albumsReducer;
